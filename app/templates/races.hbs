<div class="container-fluid">
  <div class="row">
    <div class="col-xs-3">
      <ul class="nav nav-pills nav-stacked">
      {{#each race in model}}
        <li>{{#link-to 'race' race}}{{race.name}}{{/link-to}}</li>
      {{/each}}
      </ul>
    </div>
    <div class="col-xs-9">
      {{outlet}}
    </div>
  </div>
</div>
